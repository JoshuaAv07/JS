'use-strict'

// For
/* Loops, iterartors, cycles: Are a control structure that reapetes various times
    and it is formed by the container, the condition and the iterator (increment) */

var nums = -20;

for (var i = 0; i >= nums; i--){
    console.log("Number:" + i);

    //debugger; //to stop the execution of a program and make the right debuggings
}